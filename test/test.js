'use strict';
var assert = require('assert');
var nodeAlipay = require('../');

describe('node-alipay node module', function () {
  it('I am starting tests', function () {
    nodeAlipay();
    assert(true, 'I am happy to be on testing...');
  });
});
